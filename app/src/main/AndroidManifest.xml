<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.krishnakartheek.myapptest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".CheckBoxLevel25" />
        <activity android:name=".CheckBoxWithTextView" />
        <activity android:name=".LisTActivity">

        </activity>
        <activity android:name=".ButterKnife" />
        <activity android:name=".LinkyFyTest" />
        <activity android:name=".ForUrl">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BottomNavigationActivity"
            android:label="@string/title_activity_bottom_navigation" />
        <activity android:name=".Level25Final" />
        <activity android:name=".CheckboxListViewHandling" />
        <activity android:name=".URLandAnchor">

        </activity>
    </application>

</manifest>